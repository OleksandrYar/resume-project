<main>
    <div class="row">
    {{#with main}}
    {{#with summary}}
    <section class="mb-3">
        <div class="h2">{{title}}</div>
        <div>
            <p class="text-muted col-6">
                {{text}}
            </p>
        </div>
    </section>
    {{/with}}
    {{#with experience}}
    <section>
        <div class="h2">{{title}}</div>
        <div>
            <p class="text-muted col-6">
                {{text}}
            </p>
        </div>
    </section>
    {{/with}}
    {{/with}}
    </div>
</main>